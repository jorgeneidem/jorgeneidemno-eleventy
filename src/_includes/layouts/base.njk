<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{ title }}</title>
        <meta name="author" content="{{ site.authorName }}">
        <meta name="description" content="{{ site.description }}">
        <meta rel="icon" href="{{ site.favicon }}" type="image/svg+xml">
        <link rel="alternate icon" href="{{ site.faviconFallback }}" type="image/x-icon">
        <link rel="mask-icon" href="{{ site.faviconMaskIcon }}" color="#333333">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
    {% include 'components/header.liquid' %}
    {{ content | safe }}        
        <script src="" async defer></script>
    </body>
</html>